services:
  - type: web
    name: reconmarket-backend
    runtime: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NPM_CONFIG_INCLUDE
        value: "dev"
